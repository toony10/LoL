/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["tailus.io", "tailwindui.com", "assets.goal.com"],
  },
};

module.exports = nextConfig;
